diff -ur zlib-1.2.13.orig/configure zlib-1.2.13/configure
--- zlib-1.2.13.orig/configure	2022-10-10 20:11:12.000000000 +0200
+++ zlib-1.2.13/configure	2022-11-06 01:17:07.371159605 +0100
@@ -130,7 +130,7 @@
     -l* | --libdir) libdir="$2"; shift; shift ;;
     -i* | --includedir) includedir="$2"; shift; shift ;;
     -s* | --shared | --enable-shared) shared=1; shift ;;
-    -t | --static) shared=0; shift ;;
+    -t | --enable-static | --static) shared=0; shift ;;
     --solo) solo=1; shift ;;
     --cover) cover=1; shift ;;
     -z* | --zprefix) zprefix=1; shift ;;
@@ -144,8 +144,7 @@
     --sanitize) sanitize=1; shift ;;
     *)
       echo "unknown option: $1" | tee -a configure.log
-      echo "$0 --help for help" | tee -a configure.log
-      leave 1;;
+      shift ;;
     esac
 done
 
