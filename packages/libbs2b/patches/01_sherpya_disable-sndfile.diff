diff -ur libbs2b-3.1.0.orig/configure libbs2b-3.1.0/configure
--- libbs2b-3.1.0.orig/configure	2009-06-04 20:17:12.000000000 +0200
+++ libbs2b-3.1.0/configure	2013-03-03 05:18:08.912579162 +0100
@@ -20116,22 +20116,6 @@
 
 fi
 
-# Checks for libraries.
-if test -n "$PKG_CONFIG" && \
-    { ($as_echo "$as_me:$LINENO: \$PKG_CONFIG --exists --print-errors \"sndfile\"") >&5
-  ($PKG_CONFIG --exists --print-errors "sndfile") 2>&5
-  ac_status=$?
-  $as_echo "$as_me:$LINENO: \$? = $ac_status" >&5
-  (exit $ac_status); }; then
-  :
-else
-
-    { { $as_echo "$as_me:$LINENO: error: Please install libsndfile." >&5
-$as_echo "$as_me: error: Please install libsndfile." >&2;}
-   { (exit 1); exit 1; }; }
-
-fi
-
 # Checks for header files.
 
 
